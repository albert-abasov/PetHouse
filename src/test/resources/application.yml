spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:9.6.8://hostname/pet
#    username: postgres
#    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL95Dialect